# TODO

## 高優先：資料安全

- [x] 匯入/還原前檢查 JSON 是否可解析。
- [x] 匯入/還原前檢查 `formatVersion`、`exportType` 與必要陣列欄位。
- [x] 匯入/還原前驗證 `checksum`，不一致時阻止執行。
- [x] 匯入/還原前驗證版本 `fullContent` 與 `contentHash` 是否一致。
- [x] 完整還原前顯示影響摘要，包含新增、覆蓋、合併、跳過與衝突數量。
- [x] 勾選「清空現有資料」時顯示目前會被清除的資料數量。
- [x] 刪除版本前顯示版本標籤、作者、時間與會同步刪除的標籤/批註數量。
- [x] 刪除專案前顯示該專案包含的 SQL 腳本、版本、標籤與批註數量。
- [x] 刪除 SQL 腳本前顯示該腳本包含的版本、標籤與批註數量。
- [x] 清空全部資料前顯示目前專案、SQL 腳本、版本、標籤與批註總量。
- [x] 高風險刪除/清空後還原流程使用一致的 modal 確認 UI。
- [x] 完整還原清空資料時使用單一 IndexedDB transaction 清除資料。
- [x] 導入流程增加完整 preview，先顯示會新增、覆蓋、跳過、合併多少版本。
- [x] 清空全部資料 modal 增加「先完整備份」按鈕。
- [x] 還原寫入階段進一步收斂成更少 transaction，降低中途失敗留下部分資料的機率。

## 高優先：拆分過大的主控制器

- [x] 將 `js/app.js` 的彈窗與提示邏輯拆到 `js/ui/dialogs.js`。
- [x] 將版本列表渲染、搜尋與右鍵選單拆到 `js/ui/versionTree.js`。
- [x] 將 Monaco 初始化、主題、快捷鍵與編輯器狀態拆到 `js/ui/editorController.js`。
- [x] 將匯入/匯出/備份/還原 UI 流程拆到 `js/ui/importExportDialogs.js`。
- [x] 保留 `js/app.js` 作為 bootstrap 與模組協調層。
- [x] 拆分後手動測試通過，確認主控制器拆分未破壞既有操作流程。

## 中優先：UI/UX

- [x] 將剩餘一般流程中的 `alert`、`confirm`、`prompt` 改成一致的 modal 或 toast。
- [x] 新增未儲存變更提示，切換專案或 SQL 腳本前避免內容遺失。
- [x] 儲存版本時顯示與上一版相比新增/刪除行數，讓使用者確認。
- [x] 版本列表新增排序選項。
- [x] 版本列表新增日期區間篩選。
- [x] 版本列表新增作者篩選。
- [x] 版本列表新增標籤篩選。
- [x] `diff.html` 顯示版本標籤、作者、時間與 SQL 腳本名稱，不只顯示版本 ID。

## 中優先：效能與大量資料

- [x] 版本列表改為分頁或 cursor lazy load，避免大量版本一次載入。
- [x] 減少資料層不必要的 `getAll()` 使用。
- [x] 大檔案 diff 顯示 loading 狀態。
- [x] 評估將大檔案 diff 移到 Web Worker，避免 UI 卡住。
- [x] 延遲載入 split diff editor，降低初始頁面載入成本。

## 中優先：安全與維護

- [x] 匯入衝突列表避免用 `innerHTML` 拼接未信任內容。
- [x] 還原檔案資訊避免用 `innerHTML` 拼接匯入檔內容。
- [x] 逐步減少其他使用者可輸入內容的 `innerHTML` 拼接。
- [x] CDN 依賴載入失敗時提供更清楚的離線/網路錯誤訊息。
- [x] 為 Monaco、sql-formatter、diff-match-patch 增加 fallback 或明確重試入口。
- [x] 將 script cache busting 文件化（目前 `?v=9`）。

## 低到中優先：產品功能

- [x] 完成標籤管理功能。
- [x] 新增版本收藏或重要版本 pin。
- [x] 新增 SQL 腳本搜尋。
- [x] 新增自動備份提醒，例如每 N 次儲存或每週提醒匯出。
- [x] 新增 smoke test checklist 或簡易測試頁，補足目前沒有自動測試的缺口。

## 手動驗證清單

- [x] 2026-05-11：編輯 SQL 後切換專案會顯示未儲存變更提示，取消後內容保留。
- [x] 2026-05-11：編輯 SQL 後切換 SQL 腳本會顯示未儲存變更提示，取消後內容保留。
- [x] 2026-05-11：保存版本、建立/刪除專案、建立/刪除 SQL 腳本、回溯版本使用一致的 modal 或 toast，不再使用原生 `alert`、`confirm`、`prompt`。
- [x] 2026-05-11：版本詳情與 `diff.html` 可正常顯示使用者輸入的標籤、作者、描述與 SQL 腳本名稱。
- [x] 2026-05-11：`diff.html` 顯示版本標籤、作者、時間與 SQL 腳本名稱。
- [x] 2026-05-12：保存版本對話框顯示與上一版相比的新增/刪除行數，並與保存後版本統計一致。
- [x] 2026-05-12：使用 `test-data/performance-large-backup.json` 還原 180 個版本後，版本列表可分頁載入並正常顯示「載入更多」。
- [x] 2026-05-12：使用 `diff.html?from=perf_v_179&to=perf_v_180` 驗證約 1 MB SQL 大檔 diff，loading 狀態與 Web Worker 比對流程正常。
- [x] 2026-05-12：完整還原覆蓋同名 SQL 腳本時，版本會 remap 到實際保留的 `scriptId`，還原後 `main` 版本列表可正常顯示。
- [x] 2026-05-12：版本列表支援排序、日期區間、作者與標籤篩選，並可重設回預設最新優先列表。
- [x] 2026-05-12：版本列表篩選後可正常顯示結果，日期欄位標註 `yyyy/mm/dd`，使用者驗證通過。
- [x] 2026-05-12：完成剩餘 TODO 實作，新增標籤管理、版本釘選、SQL 腳本搜尋、自動備份提醒、CDN 載入失敗重試入口與 `smoke-test.html` 清單；已執行靜態語法檢查。
- [ ] 建立專案、SQL 腳本、版本後完整備份。
- [ ] 使用完整備份還原，確認專案、腳本、版本、標籤、批註可正常回來。
- [ ] 在已有資料下還原同一份備份，測試 `skip`、`overwrite`、`merge` 三種策略。
- [ ] 勾選「清除既有資料」後還原，確認摘要與結果正確。
- [ ] 使用非 JSON 檔案測試匯入/還原拒絕流程。
- [ ] 使用缺少必要欄位的 JSON 測試拒絕流程。
- [ ] 使用 checksum 不一致的 JSON 測試拒絕流程。
- [ ] 使用 `fullContent` 與 `contentHash` 不一致的 JSON 測試拒絕流程。
- [ ] 刪除版本時確認標籤與批註同步刪除。
- [ ] 刪除 SQL 腳本時確認版本、標籤、批註同步刪除。
- [ ] 刪除專案時確認 SQL 腳本、版本、標籤、批註同步刪除。
